from setuptools import setup

install_requires = [
]

packages = [
    "py_strict_list"
]

setup(
    name='py_strict_list',
    version='0.0.0',
    packages=packages,
    install_requires=install_requires,
    test_suite='test'
)