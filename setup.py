from setuptools import setup

install_requires = [
]

packages = [
    "py_strict_list"
]

setup(
    name='py-strict-list',
    version='0.0.0',
    packages=packages,
    install_requires=install_requires,
)